{
  "name": "automation-bot",
  "version": "1.0.0",
  "description": "This bot generates or fetches quotes from API and generates a image using that qoute and downloads the images and saves the quote in your host machine",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "author": "Abhishek Gaire",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^23.7.1"
  }
}
